const { courses, users, users_courses, testFunctions } = require('../../server/models')

describe('secondary courses', () => {

  test('should retrieve correct discussion sections for desired lecture', () => {
    expect(1).toBe(1)
  })
})
